{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for AppManager desktop windows.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen"
  ]
}
